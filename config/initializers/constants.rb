
  #check to see if e-mail is valid format
  EMAIL_REGEX = /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,4}$/i

  #checks to see if only letters and numbers, but no special characters
  ALPHA_NUMERIC_REGEX = /^\d*[a-zA-Z][a-zA-Z\d]*$/
